import { IBotConfig } from "../Interfaces";

export const config: IBotConfig = {
    token: "", // DiscordBot token
    prefix: "!", // Command prefix
    botOwnerRoleName: "", // The role assigned to the bot
    enableReactions: true,
    baseUrl: "", // Base url for the bot to reach out
    color: 327424, // Embed line color
};
